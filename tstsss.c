#include <stdio.h>
#include "cube.h"

int main(int ac, char *av[])
{
    // t_var var;

    // // printf("floor(10) == %f\n",floor(10.00000));

    // parsing(ac, av, &var);
    // _init_window(&var);
    // init_mini_map(&var);
    // draw_vector(&var);
    // cast(&var);
    // mlx_key_hook(var.mlx, &listen_to_key, (void*)&var);
    // mlx_loop(var.mlx);
    int p[4];
    p[5] = 20;
    printf("p[7] === %d\n",p[5]);
    return 0;
}
